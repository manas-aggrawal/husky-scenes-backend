export enum Role {
  ADMIN = 'ADMIN',
  USER = 'USER',
  COMMUNITY = 'COMMUNITY',
  PUBLIC = 'PUBLIC',
}

export enum EventCategory {
  SPORTS = 'SPORTS',
  ACADEMIC = 'ACADEMIC',
  SOCIAL = 'SOCIAL',
}

// to be decided
export enum EventTags {}
